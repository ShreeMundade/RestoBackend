/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE4Args } from "./DeleteE4Args";
import { E4FindManyArgs } from "./E4FindManyArgs";
import { E4FindUniqueArgs } from "./E4FindUniqueArgs";
import { E4 } from "./E4";
import { E4Service } from "../e4.service";

@graphql.Resolver(() => E4)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E4ResolverBase {
  constructor(
    protected readonly service: E4Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E4",
    action: "read",
    possession: "any",
  })
  async _e4sMeta(
    @graphql.Args() args: E4FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E4])
  @nestAccessControl.UseRoles({
    resource: "E4",
    action: "read",
    possession: "any",
  })
  async e4s(@graphql.Args() args: E4FindManyArgs): Promise<E4[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E4, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E4",
    action: "read",
    possession: "own",
  })
  async e4(@graphql.Args() args: E4FindUniqueArgs): Promise<E4 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E4)
  @nestAccessControl.UseRoles({
    resource: "E4",
    action: "delete",
    possession: "any",
  })
  async deleteE4(@graphql.Args() args: DeleteE4Args): Promise<E4 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
