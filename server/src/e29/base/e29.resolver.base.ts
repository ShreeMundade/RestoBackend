/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE29Args } from "./DeleteE29Args";
import { E29FindManyArgs } from "./E29FindManyArgs";
import { E29FindUniqueArgs } from "./E29FindUniqueArgs";
import { E29 } from "./E29";
import { E29Service } from "../e29.service";

@graphql.Resolver(() => E29)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E29ResolverBase {
  constructor(
    protected readonly service: E29Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E29",
    action: "read",
    possession: "any",
  })
  async _e29sMeta(
    @graphql.Args() args: E29FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E29])
  @nestAccessControl.UseRoles({
    resource: "E29",
    action: "read",
    possession: "any",
  })
  async e29s(@graphql.Args() args: E29FindManyArgs): Promise<E29[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E29, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E29",
    action: "read",
    possession: "own",
  })
  async e29(@graphql.Args() args: E29FindUniqueArgs): Promise<E29 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E29)
  @nestAccessControl.UseRoles({
    resource: "E29",
    action: "delete",
    possession: "any",
  })
  async deleteE29(@graphql.Args() args: DeleteE29Args): Promise<E29 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
