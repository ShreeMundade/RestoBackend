/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE57Args } from "./DeleteE57Args";
import { E57FindManyArgs } from "./E57FindManyArgs";
import { E57FindUniqueArgs } from "./E57FindUniqueArgs";
import { E57 } from "./E57";
import { E57Service } from "../e57.service";

@graphql.Resolver(() => E57)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E57ResolverBase {
  constructor(
    protected readonly service: E57Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E57",
    action: "read",
    possession: "any",
  })
  async _e57sMeta(
    @graphql.Args() args: E57FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E57])
  @nestAccessControl.UseRoles({
    resource: "E57",
    action: "read",
    possession: "any",
  })
  async e57s(@graphql.Args() args: E57FindManyArgs): Promise<E57[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E57, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E57",
    action: "read",
    possession: "own",
  })
  async e57(@graphql.Args() args: E57FindUniqueArgs): Promise<E57 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E57)
  @nestAccessControl.UseRoles({
    resource: "E57",
    action: "delete",
    possession: "any",
  })
  async deleteE57(@graphql.Args() args: DeleteE57Args): Promise<E57 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
