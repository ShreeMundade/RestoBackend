/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE20Args } from "./DeleteE20Args";
import { E20FindManyArgs } from "./E20FindManyArgs";
import { E20FindUniqueArgs } from "./E20FindUniqueArgs";
import { E20 } from "./E20";
import { E20Service } from "../e20.service";

@graphql.Resolver(() => E20)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E20ResolverBase {
  constructor(
    protected readonly service: E20Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E20",
    action: "read",
    possession: "any",
  })
  async _e20sMeta(
    @graphql.Args() args: E20FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E20])
  @nestAccessControl.UseRoles({
    resource: "E20",
    action: "read",
    possession: "any",
  })
  async e20s(@graphql.Args() args: E20FindManyArgs): Promise<E20[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E20, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E20",
    action: "read",
    possession: "own",
  })
  async e20(@graphql.Args() args: E20FindUniqueArgs): Promise<E20 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E20)
  @nestAccessControl.UseRoles({
    resource: "E20",
    action: "delete",
    possession: "any",
  })
  async deleteE20(@graphql.Args() args: DeleteE20Args): Promise<E20 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
