/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE10Args } from "./DeleteE10Args";
import { E10FindManyArgs } from "./E10FindManyArgs";
import { E10FindUniqueArgs } from "./E10FindUniqueArgs";
import { E10 } from "./E10";
import { E10Service } from "../e10.service";

@graphql.Resolver(() => E10)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E10ResolverBase {
  constructor(
    protected readonly service: E10Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E10",
    action: "read",
    possession: "any",
  })
  async _e10sMeta(
    @graphql.Args() args: E10FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E10])
  @nestAccessControl.UseRoles({
    resource: "E10",
    action: "read",
    possession: "any",
  })
  async e10s(@graphql.Args() args: E10FindManyArgs): Promise<E10[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E10, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E10",
    action: "read",
    possession: "own",
  })
  async e10(@graphql.Args() args: E10FindUniqueArgs): Promise<E10 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E10)
  @nestAccessControl.UseRoles({
    resource: "E10",
    action: "delete",
    possession: "any",
  })
  async deleteE10(@graphql.Args() args: DeleteE10Args): Promise<E10 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
