/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE48Args } from "./DeleteE48Args";
import { E48FindManyArgs } from "./E48FindManyArgs";
import { E48FindUniqueArgs } from "./E48FindUniqueArgs";
import { E48 } from "./E48";
import { E48Service } from "../e48.service";

@graphql.Resolver(() => E48)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E48ResolverBase {
  constructor(
    protected readonly service: E48Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E48",
    action: "read",
    possession: "any",
  })
  async _e48sMeta(
    @graphql.Args() args: E48FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E48])
  @nestAccessControl.UseRoles({
    resource: "E48",
    action: "read",
    possession: "any",
  })
  async e48s(@graphql.Args() args: E48FindManyArgs): Promise<E48[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E48, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E48",
    action: "read",
    possession: "own",
  })
  async e48(@graphql.Args() args: E48FindUniqueArgs): Promise<E48 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E48)
  @nestAccessControl.UseRoles({
    resource: "E48",
    action: "delete",
    possession: "any",
  })
  async deleteE48(@graphql.Args() args: DeleteE48Args): Promise<E48 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
