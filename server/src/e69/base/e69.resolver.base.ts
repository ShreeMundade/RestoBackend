/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE69Args } from "./DeleteE69Args";
import { E69FindManyArgs } from "./E69FindManyArgs";
import { E69FindUniqueArgs } from "./E69FindUniqueArgs";
import { E69 } from "./E69";
import { E69Service } from "../e69.service";

@graphql.Resolver(() => E69)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E69ResolverBase {
  constructor(
    protected readonly service: E69Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E69",
    action: "read",
    possession: "any",
  })
  async _e69sMeta(
    @graphql.Args() args: E69FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E69])
  @nestAccessControl.UseRoles({
    resource: "E69",
    action: "read",
    possession: "any",
  })
  async e69s(@graphql.Args() args: E69FindManyArgs): Promise<E69[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E69, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E69",
    action: "read",
    possession: "own",
  })
  async e69(@graphql.Args() args: E69FindUniqueArgs): Promise<E69 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E69)
  @nestAccessControl.UseRoles({
    resource: "E69",
    action: "delete",
    possession: "any",
  })
  async deleteE69(@graphql.Args() args: DeleteE69Args): Promise<E69 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
