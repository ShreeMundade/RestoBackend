/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE13Args } from "./DeleteE13Args";
import { E13FindManyArgs } from "./E13FindManyArgs";
import { E13FindUniqueArgs } from "./E13FindUniqueArgs";
import { E13 } from "./E13";
import { E13Service } from "../e13.service";

@graphql.Resolver(() => E13)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E13ResolverBase {
  constructor(
    protected readonly service: E13Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E13",
    action: "read",
    possession: "any",
  })
  async _e13sMeta(
    @graphql.Args() args: E13FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E13])
  @nestAccessControl.UseRoles({
    resource: "E13",
    action: "read",
    possession: "any",
  })
  async e13s(@graphql.Args() args: E13FindManyArgs): Promise<E13[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E13, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E13",
    action: "read",
    possession: "own",
  })
  async e13(@graphql.Args() args: E13FindUniqueArgs): Promise<E13 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E13)
  @nestAccessControl.UseRoles({
    resource: "E13",
    action: "delete",
    possession: "any",
  })
  async deleteE13(@graphql.Args() args: DeleteE13Args): Promise<E13 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
