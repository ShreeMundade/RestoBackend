/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE44Args } from "./DeleteE44Args";
import { E44FindManyArgs } from "./E44FindManyArgs";
import { E44FindUniqueArgs } from "./E44FindUniqueArgs";
import { E44 } from "./E44";
import { E44Service } from "../e44.service";

@graphql.Resolver(() => E44)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E44ResolverBase {
  constructor(
    protected readonly service: E44Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E44",
    action: "read",
    possession: "any",
  })
  async _e44sMeta(
    @graphql.Args() args: E44FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E44])
  @nestAccessControl.UseRoles({
    resource: "E44",
    action: "read",
    possession: "any",
  })
  async e44s(@graphql.Args() args: E44FindManyArgs): Promise<E44[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E44, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E44",
    action: "read",
    possession: "own",
  })
  async e44(@graphql.Args() args: E44FindUniqueArgs): Promise<E44 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E44)
  @nestAccessControl.UseRoles({
    resource: "E44",
    action: "delete",
    possession: "any",
  })
  async deleteE44(@graphql.Args() args: DeleteE44Args): Promise<E44 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
