/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE16Args } from "./DeleteE16Args";
import { E16FindManyArgs } from "./E16FindManyArgs";
import { E16FindUniqueArgs } from "./E16FindUniqueArgs";
import { E16 } from "./E16";
import { E16Service } from "../e16.service";

@graphql.Resolver(() => E16)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E16ResolverBase {
  constructor(
    protected readonly service: E16Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E16",
    action: "read",
    possession: "any",
  })
  async _e16sMeta(
    @graphql.Args() args: E16FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E16])
  @nestAccessControl.UseRoles({
    resource: "E16",
    action: "read",
    possession: "any",
  })
  async e16s(@graphql.Args() args: E16FindManyArgs): Promise<E16[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E16, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E16",
    action: "read",
    possession: "own",
  })
  async e16(@graphql.Args() args: E16FindUniqueArgs): Promise<E16 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E16)
  @nestAccessControl.UseRoles({
    resource: "E16",
    action: "delete",
    possession: "any",
  })
  async deleteE16(@graphql.Args() args: DeleteE16Args): Promise<E16 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
