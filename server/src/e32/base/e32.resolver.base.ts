/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE32Args } from "./DeleteE32Args";
import { E32FindManyArgs } from "./E32FindManyArgs";
import { E32FindUniqueArgs } from "./E32FindUniqueArgs";
import { E32 } from "./E32";
import { E32Service } from "../e32.service";

@graphql.Resolver(() => E32)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E32ResolverBase {
  constructor(
    protected readonly service: E32Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E32",
    action: "read",
    possession: "any",
  })
  async _e32sMeta(
    @graphql.Args() args: E32FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E32])
  @nestAccessControl.UseRoles({
    resource: "E32",
    action: "read",
    possession: "any",
  })
  async e32s(@graphql.Args() args: E32FindManyArgs): Promise<E32[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E32, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E32",
    action: "read",
    possession: "own",
  })
  async e32(@graphql.Args() args: E32FindUniqueArgs): Promise<E32 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E32)
  @nestAccessControl.UseRoles({
    resource: "E32",
    action: "delete",
    possession: "any",
  })
  async deleteE32(@graphql.Args() args: DeleteE32Args): Promise<E32 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
