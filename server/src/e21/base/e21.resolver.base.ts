/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE21Args } from "./DeleteE21Args";
import { E21FindManyArgs } from "./E21FindManyArgs";
import { E21FindUniqueArgs } from "./E21FindUniqueArgs";
import { E21 } from "./E21";
import { E21Service } from "../e21.service";

@graphql.Resolver(() => E21)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E21ResolverBase {
  constructor(
    protected readonly service: E21Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E21",
    action: "read",
    possession: "any",
  })
  async _e21sMeta(
    @graphql.Args() args: E21FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E21])
  @nestAccessControl.UseRoles({
    resource: "E21",
    action: "read",
    possession: "any",
  })
  async e21s(@graphql.Args() args: E21FindManyArgs): Promise<E21[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E21, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E21",
    action: "read",
    possession: "own",
  })
  async e21(@graphql.Args() args: E21FindUniqueArgs): Promise<E21 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E21)
  @nestAccessControl.UseRoles({
    resource: "E21",
    action: "delete",
    possession: "any",
  })
  async deleteE21(@graphql.Args() args: DeleteE21Args): Promise<E21 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
