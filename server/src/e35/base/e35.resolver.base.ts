/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE35Args } from "./DeleteE35Args";
import { E35FindManyArgs } from "./E35FindManyArgs";
import { E35FindUniqueArgs } from "./E35FindUniqueArgs";
import { E35 } from "./E35";
import { E35Service } from "../e35.service";

@graphql.Resolver(() => E35)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E35ResolverBase {
  constructor(
    protected readonly service: E35Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E35",
    action: "read",
    possession: "any",
  })
  async _e35sMeta(
    @graphql.Args() args: E35FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E35])
  @nestAccessControl.UseRoles({
    resource: "E35",
    action: "read",
    possession: "any",
  })
  async e35s(@graphql.Args() args: E35FindManyArgs): Promise<E35[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E35, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E35",
    action: "read",
    possession: "own",
  })
  async e35(@graphql.Args() args: E35FindUniqueArgs): Promise<E35 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E35)
  @nestAccessControl.UseRoles({
    resource: "E35",
    action: "delete",
    possession: "any",
  })
  async deleteE35(@graphql.Args() args: DeleteE35Args): Promise<E35 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
