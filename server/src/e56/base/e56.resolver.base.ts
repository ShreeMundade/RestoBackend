/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE56Args } from "./DeleteE56Args";
import { E56FindManyArgs } from "./E56FindManyArgs";
import { E56FindUniqueArgs } from "./E56FindUniqueArgs";
import { E56 } from "./E56";
import { E56Service } from "../e56.service";

@graphql.Resolver(() => E56)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E56ResolverBase {
  constructor(
    protected readonly service: E56Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E56",
    action: "read",
    possession: "any",
  })
  async _e56sMeta(
    @graphql.Args() args: E56FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E56])
  @nestAccessControl.UseRoles({
    resource: "E56",
    action: "read",
    possession: "any",
  })
  async e56s(@graphql.Args() args: E56FindManyArgs): Promise<E56[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E56, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E56",
    action: "read",
    possession: "own",
  })
  async e56(@graphql.Args() args: E56FindUniqueArgs): Promise<E56 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E56)
  @nestAccessControl.UseRoles({
    resource: "E56",
    action: "delete",
    possession: "any",
  })
  async deleteE56(@graphql.Args() args: DeleteE56Args): Promise<E56 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
