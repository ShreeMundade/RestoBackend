/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE7Args } from "./DeleteE7Args";
import { E7FindManyArgs } from "./E7FindManyArgs";
import { E7FindUniqueArgs } from "./E7FindUniqueArgs";
import { E7 } from "./E7";
import { E7Service } from "../e7.service";

@graphql.Resolver(() => E7)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E7ResolverBase {
  constructor(
    protected readonly service: E7Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E7",
    action: "read",
    possession: "any",
  })
  async _e7sMeta(
    @graphql.Args() args: E7FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E7])
  @nestAccessControl.UseRoles({
    resource: "E7",
    action: "read",
    possession: "any",
  })
  async e7s(@graphql.Args() args: E7FindManyArgs): Promise<E7[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E7, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E7",
    action: "read",
    possession: "own",
  })
  async e7(@graphql.Args() args: E7FindUniqueArgs): Promise<E7 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E7)
  @nestAccessControl.UseRoles({
    resource: "E7",
    action: "delete",
    possession: "any",
  })
  async deleteE7(@graphql.Args() args: DeleteE7Args): Promise<E7 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
