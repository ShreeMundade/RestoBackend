/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE46Args } from "./DeleteE46Args";
import { E46FindManyArgs } from "./E46FindManyArgs";
import { E46FindUniqueArgs } from "./E46FindUniqueArgs";
import { E46 } from "./E46";
import { E46Service } from "../e46.service";

@graphql.Resolver(() => E46)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E46ResolverBase {
  constructor(
    protected readonly service: E46Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E46",
    action: "read",
    possession: "any",
  })
  async _e46sMeta(
    @graphql.Args() args: E46FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E46])
  @nestAccessControl.UseRoles({
    resource: "E46",
    action: "read",
    possession: "any",
  })
  async e46s(@graphql.Args() args: E46FindManyArgs): Promise<E46[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E46, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E46",
    action: "read",
    possession: "own",
  })
  async e46(@graphql.Args() args: E46FindUniqueArgs): Promise<E46 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E46)
  @nestAccessControl.UseRoles({
    resource: "E46",
    action: "delete",
    possession: "any",
  })
  async deleteE46(@graphql.Args() args: DeleteE46Args): Promise<E46 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
