/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as graphql from "@nestjs/graphql";
import * as apollo from "apollo-server-express";
import * as nestAccessControl from "nest-access-control";
import { GqlDefaultAuthGuard } from "../../auth/gqlDefaultAuth.guard";
import * as gqlACGuard from "../../auth/gqlAC.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DeleteE38Args } from "./DeleteE38Args";
import { E38FindManyArgs } from "./E38FindManyArgs";
import { E38FindUniqueArgs } from "./E38FindUniqueArgs";
import { E38 } from "./E38";
import { E38Service } from "../e38.service";

@graphql.Resolver(() => E38)
@common.UseGuards(GqlDefaultAuthGuard, gqlACGuard.GqlACGuard)
export class E38ResolverBase {
  constructor(
    protected readonly service: E38Service,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @graphql.Query(() => MetaQueryPayload)
  @nestAccessControl.UseRoles({
    resource: "E38",
    action: "read",
    possession: "any",
  })
  async _e38sMeta(
    @graphql.Args() args: E38FindManyArgs
  ): Promise<MetaQueryPayload> {
    const results = await this.service.count({
      ...args,
      skip: undefined,
      take: undefined,
    });
    return {
      count: results,
    };
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => [E38])
  @nestAccessControl.UseRoles({
    resource: "E38",
    action: "read",
    possession: "any",
  })
  async e38s(@graphql.Args() args: E38FindManyArgs): Promise<E38[]> {
    return this.service.findMany(args);
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @graphql.Query(() => E38, { nullable: true })
  @nestAccessControl.UseRoles({
    resource: "E38",
    action: "read",
    possession: "own",
  })
  async e38(@graphql.Args() args: E38FindUniqueArgs): Promise<E38 | null> {
    const result = await this.service.findOne(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => E38)
  @nestAccessControl.UseRoles({
    resource: "E38",
    action: "delete",
    possession: "any",
  })
  async deleteE38(@graphql.Args() args: DeleteE38Args): Promise<E38 | null> {
    try {
      return await this.service.delete(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new apollo.ApolloError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
